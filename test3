SELECT a FROM(SELECT  G.GoODS_ID, G.SN, G.NAME, C.NAME  CNAME FROM(SELECT DISTINCT  GOODS_ID FROM  ES_PRODUCT P WHERE  P.PRODUCT_ID NOT  IN(SELECT  PRODUCTID FROM(SELECT  PRODUCTID, FROM  ES_PRODUCT_STORE WHERE  ENABLE_STORE>5 GROUP BY  PRODUCTID)TEM WHERE  COUNT=(SELECT a FROM  ES_DEPOT)))TEM, ES_GOODS G, ES_GOODS_CAT C WHERE  G.CAT_ID=C.CAT_ID AND  G.GOODS_ID=TEM.GOODS_ID AND  G.MARKET_ENABLE=1 AND  G.DISABLED=0)TEMP_TABLE;
